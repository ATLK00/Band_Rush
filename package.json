{
  "name": "band-rush",
  "version": "1.0.0",
  "scripts": {
    "dev:server": "cd server && npm install && npm start",
    "dev:client": "cd client && npm install && npm start",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "start": "node server/index.js"
  },
  "description": "Band Rush - Real-time Music Instrument Party Game"
}
